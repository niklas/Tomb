#!/usr/bin/ruby

require File.expand_path(File.dirname(__FILE__) + '/../config/environment')


Gibak::Build.build
gibak = Gibak::Base.new

gibak.ls_changed_files do |file|
  puts "changed: #{file}"
end

Gibak::Mount.umount
